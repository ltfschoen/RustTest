/* Import generics library crate into scope
 */
extern crate generics;

fn main() {
    // generics::examples::function_definitions::run();
    // generics::examples::enum_definitions::run();
    generics::examples::method_definitions::run();
}
