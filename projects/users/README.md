# Shapes

* Execution

```
cd projects/users;
cargo run;
cargo doc --open;
```

* Tests

```
cd projects/users;
cargo build;
cargo test;
```

* Notes

Originally setup with:

```
mkdir projects
cd projects
cargo new users --bin
cd users
```