# Shapes

* Execution

```
cd projects/shapes;
cargo build;
./target/debug/shapes;
cargo doc --open;
```

* Tests

```
cd projects/shapes;
cargo build;
cargo test;
```

* Notes

Originally setup with:

```
mkdir projects
cd projects
cargo new shapes --bin
cd shapes
```